useEffect(() => {
  const template = storageService.getApplicationDataByKey(
    `${EnvDetail.ParentAppName}-${EnvDetail.AppName}`,
    "ReportTemplate"
  );

  if (template) {
    setSelectedTemplate(template);
  }
}, []);
