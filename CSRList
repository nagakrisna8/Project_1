useEffect(() => {
    if (selectedTemplate && Object.keys(selectedTemplate).length > 0) {
        const storageService = new StorageService();

        storageService.setApplicationDataByKey(
            `${EnvDetail.ParentAppName}-${EnvDetail.AppName}`,
            "reportTemplate",
            selectedTemplate   // ✅ FULL OBJECT stored
        );

        console.log("✅ Stored FULL reportTemplate:", selectedTemplate);
    }
}, [selectedTemplate]);
