useEffect(() => {
  if (getPurposeList.length > 0 && selectedValue) {
    getSearchData(
      searchId || 0,
      selectedValue,
      dataState,
      getPurposeList,
      selectedItem
    );
  }
}, [getPurposeList]);
