useEffect(() => {
  const newValue = dataItem[field];

  // ✅ only update if API gives valid value
  if (newValue !== undefined && newValue !== null && newValue !== "") {
    setInputValue(newValue);
  }
}, [dataItem, field]);
