
                if (!csrHeaderData[0].SupplierName?.trim()) {
                    newErrors.SupplierName = "* Organization name is required"; // ✅ changed message
                    validationFlag = validationFlag && false;
                } else {
                    newErrors.SupplierName = "";
                    validationFlag = validationFlag && true;
                }
