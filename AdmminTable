export default function Overview() {
    const {
        ccnData,
        cclData,
        cpData,
        refreshDrawerData,
        markedAsCompleteData
    } = useOutletContext<OverallDataContext>();

    // While API is loading show white background
    if (!ccnData || Object.keys(ccnData).length === 0) {
        return (
            <div
                style={{
                    backgroundColor: "#fff",
                    minHeight: "100vh",
                    width: "100%"
                }}
            />
        );
    }

    return (
        <>
            {
                (!ccnData.status || ccnData.status === "") ?
                    (
                        <NoCCNExists />
                    )
                    :
                    (
                        <div className="oss-ccl-overview-outer">
                            <div className="oss-ccl-overview-title">
                                Cert Strategy
                            </div>

                            <div className="oss-ccl-overview-cards">
                                <Cards
                                    ccn={ccnData}
                                    ccl={cclData}
                                    cp={cpData}
                                    markedAsCompleteData={markedAsCompleteData}
                                />
                            </div>

                            <div className="oss-ccl-overview-tcds">
                                <Tcds />
                            </div>

                            <div className="oss-ccl-overview-as">
                                <AuthoringSequence
                                    ccn={ccnData}
                                    ccl={cclData}
                                    cp={cpData}
                                    markedAsCompleteData={markedAsCompleteData}
                                />
                            </div>
                        </div>
                    )
            }
        </>
    );
}
