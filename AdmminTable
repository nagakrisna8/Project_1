 if (!csrHeaderData[0]?.FocalRecipientList || csrHeaderData[0].FocalRecipientList.length === 0) {
        newErrors.FocalRecipient = "* Focal recipient is required";
        validationFlag = false;
    } else {
        newErrors.FocalRecipient = "";
    }

{errors.FocalRecipient && (
    <div style={{ color: "red", fontSize: "12px" }}>
        {errors.FocalRecipient}
    </div>
)}
