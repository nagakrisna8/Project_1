else if (window.location.href.indexOf(WorkAppRoutes.FIND_CSR.CSR_LIST_SEARCH.replace("/:groupId", "").replace("/:registerId", "")) > -1) {
    const existingBreadcrumb = document.getElementById('oss-menu-breadcrums');
    const alreadyRendered = existingBreadcrumb && existingBreadcrumb.children.length > 0;
    if (!alreadyRendered) {
        handleBreadcrumsNavigation("CSR");
    }
}
