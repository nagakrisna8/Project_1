InsertSupportingDetailData = (response.data.GridResult || []).map((item) => {
  return {
    ...item,
    PURPOSE: item.PURPOSE || "",
    STATUS: item.STATUS || ""
  };
});


setData(InsertSupportingDetailData || []);
