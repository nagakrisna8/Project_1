// inside fetchInitialSupInsertData

if (String(isTemplate) === "DDTL") {
  getSearchData(null, 9, dataState, regPurposeData.data, configRefDataResult.data.RefDetailsList);
} else {
  getSearchData(0, refDataResponse.data[getDCSourceIndex].SourceId, dataState, regPurposeData.data, configRefDataResult.data.RefDetailsList);
}
