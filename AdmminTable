useEffect(() => {
  if (getPurposeList.length > 0 && getData.length > 0) {
    setData(prev =>
      prev.map(item => ({
        ...item,
        PURPOSE: item.PURPOSE || ""
      }))
    );
  }
}, [getPurposeList]);

// ✅ ADD THIS BLOCK (after for-loop)
InsertSupportingDetailData = InsertSupportingDetailData.map(item => ({
  ...item,
  PURPOSE: item.PURPOSE || ""
}));
