cells={{
  data: (props: any) => (
    <CustomDropDown {...props} getPurposeList={getPurposeList} />
  )
}}

const CustomDropDown = (props) => {
  const { dataItem, field = '', getPurposeList } = props;
