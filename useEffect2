import React, { memo, useCallback, useEffect, useRef, useState } from "react";
import { OrganizationTableData, SortByData } from "../AdminModal";
import { getter, State, process } from "@progress/kendo-data-query";
import {
  Grid as DataGrid,
  GridCellProps,
  GridColumn,
  GridColumnMenuFilter,
  GridColumnMenuProps,
  GridDataStateChangeEvent,
  GridItemChangeEvent,
  GridToolbar
} from "@progress/kendo-react-grid";

import {
  Button,
  Card,
  MenuItem,
  Select,
  TextField,
  Typography
} from "@mui/material";

import AddCircleOutlineIcon from "@mui/icons-material/AddCircleOutline";
import SettingsIcon from "@mui/icons-material/Settings";
import ArrowUpwardIcon from "@mui/icons-material/ArrowUpward";
import ArrowDownwardIcon from "@mui/icons-material/ArrowDownward";
import SortIcon from "@mui/icons-material/Sort";
import GroupIcon from "@mui/icons-material/Group";
import ArrowDropDownIcon from "@mui/icons-material/ArrowDropDown";
import ArrowDropUpIcon from "@mui/icons-material/ArrowDropUp";

import { filterIcon } from "@progress/kendo-svg-icons";
import { useDispatch } from "react-redux";

import FocalList from "./FocalList";

const editField = "inEdit";
const SELECTED_FIELD: string = "selected";
const AnyGrid = DataGrid as any;

const GridFilterMenuWithoutSort = (props: GridColumnMenuProps) => (
  <div>
    <GridColumnMenuFilter {...props} expanded={true} />
  </div>
);

interface OrganizationTypeTableProps {
  KEY: string;
  columnPropsBuilder: (field: any) => {
    field: any;
    columnMenu: (props: any) => JSX.Element;
    headerClassName: string;
  };
  organizationData: OrganizationTableData[];
  dataState: State;
  dataStateChange: (event: GridDataStateChangeEvent) => void;
  itemChange: (event: GridItemChangeEvent) => void;
  createOrganization: () => void;
  saveOrganization: () => void;
  dragStart: (dataItem: OrganizationTableData) => void;
  reorder: (dataItem: OrganizationTableData) => void;
  sortBy: "custom" | "asc" | "desc" | undefined;
  handleSortByChange: (
    key: keyof SortByData,
    value: "custom" | "asc" | "desc"
  ) => void;
}

const OrganizationTable: React.FC<OrganizationTypeTableProps> = memo(
  (props) => {
    const dispatch = useDispatch();
    const gridRefBuilder = useRef<any>(null);

    const [expandedRows, setExpandedRows] = useState<any>({});

    const OrganizationCell: React.ComponentType<GridCellProps> = useCallback(
      (cellProps) => {
        const { dataItem } = cellProps;
        const field = cellProps.field || "";
        const dataValue = dataItem[field] ?? "";

        const handleChange = (event: any) => {
          if (cellProps.onChange) {
            const updatedDataItem = { ...cellProps.dataItem };
            updatedDataItem.ORGANIZATIONNAME = event.target.value;

            cellProps.onChange({
              dataIndex: cellProps.dataIndex,
              dataItem: updatedDataItem,
              field: cellProps.field,
              syntheticEvent: event,
              value: event.target.value
            });
          }
        };

        return (
          <td title={dataValue}>
            <TextField
              fullWidth
              value={dataValue}
              onChange={handleChange}
            />
          </td>
        );
      },
      []
    );

    const DragCell: React.ComponentType<GridCellProps> = (propsDrag) => (
      <td
        onDragOver={(e) => {
          e.preventDefault();
          props.reorder(propsDrag.dataItem);
        }}
      >
        <span
          draggable
          style={{ cursor: "move", display: "flex", justifyContent: "center" }}
          onDragStart={() => props.dragStart(propsDrag.dataItem)}
        >
          <SortIcon />
        </span>
      </td>
    );

    const ContactFocals: React.ComponentType<GridCellProps> = (cellProps) => {
      const rowKey = cellProps.dataItem[props.KEY];
      const isOpen = expandedRows[rowKey];

      const toggle = () => {
        setExpandedRows((prev: any) => ({
          ...prev,
          [rowKey]: !prev[rowKey]
        }));
      };

      return (
        <td>
          <span
            style={{
              display: "flex",
              alignItems: "center",
              gap: 10,
              marginLeft: 20,
              cursor: "pointer"
            }}
            onClick={toggle}
          >
            <GroupIcon />

            {isOpen ? <ArrowDropUpIcon /> : <ArrowDropDownIcon />}
          </span>
        </td>
      );
    };

    const DetailComponent = (detailProps: any) => {
      const rowKey = detailProps.dataItem[props.KEY];

      if (!expandedRows[rowKey]) return null;

      return (
        <div style={{ padding: 15, background: "#fafafa" }}>
          <FocalList organizationId={rowKey} />
        </div>
      );
    };

    const activeCheckbox: React.ComponentType<GridCellProps> = (cellProps) => {
      const dataItem = cellProps.dataItem;

      const handleChange = (e: any) => {
        const updated = { ...dataItem, ISACTIVE: e.target.checked };

        cellProps.onChange?.({
          dataIndex: cellProps.dataIndex,
          dataItem: updated,
          field: cellProps.field,
          syntheticEvent: {} as React.SyntheticEvent,
          value: e.target.value
        });
      };

      return (
        <td>
          <div style={{ display: "flex", justifyContent: "center" }}>
            <input
              type="checkbox"
              checked={dataItem.ISACTIVE}
              onChange={handleChange}
            />
          </div>
        </td>
      );
    };

    return (
      <div style={{ paddingBottom: 20 }}>
        <Card style={{ borderLeft: "4px solid #0033A1", marginLeft: 22 }}>
          <AnyGrid
            ref={gridRefBuilder}
            style={{ height: "60vh" }}
            data={process(props.organizationData, props.dataState).data}
            editField={editField}
            dataItemKey={props.KEY}
            resizable
            {...props.dataState}
            selectedField={SELECTED_FIELD}
            onDataStateChange={props.dataStateChange}
            onItemChange={props.itemChange}
            columnMenuIcon={filterIcon}
            detail={DetailComponent}
          >
            <GridToolbar>
              <div
                style={{
                  display: "flex",
                  justifyContent: "space-between",
                  width: "100%"
                }}
              >
                <div style={{ display: "flex", gap: 8 }}>
                  <Button
                    startIcon={<AddCircleOutlineIcon />}
                    onClick={props.createOrganization}
                    variant="outlined"
                    size="small"
                  >
                    Create New
                  </Button>

                  <Button
                    onClick={props.saveOrganization}
                    variant="outlined"
                    size="small"
                  >
                    Save
                  </Button>
                </div>

                <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
                  Sort Order :

                  <Select
                    value={props.sortBy || "custom"}
                    style={{ width: 160 }}
                    onChange={(e) =>
                      props.handleSortByChange(
                        "organization",
                        e.target.value as any
                      )
                    }
                  >
                    <MenuItem value="custom">
                      <SettingsIcon /> Customize
                    </MenuItem>
                    <MenuItem value="asc">
                      <ArrowUpwardIcon /> Ascending
                    </MenuItem>
                    <MenuItem value="desc">
                      <ArrowDownwardIcon /> Descending
                    </MenuItem>
                  </Select>
                </div>
              </div>
            </GridToolbar>

            <GridColumn title="Sort" cells={{ data: DragCell }} width="90px" />

            <GridColumn
              {...props.columnPropsBuilder("ORGANIZATIONNAME")}
              title="Organization Name"
              cells={{ data: OrganizationCell }}
              width="300px"
              columnMenu={GridFilterMenuWithoutSort}
            />

            <GridColumn
              {...props.columnPropsBuilder("ORGANIZATIONDESCRIPTION")}
              title="MOA Number"
              cells={{ data: OrganizationCell }}
              width="240px"
            />

            <GridColumn
              {...props.columnPropsBuilder("ISACTIVE")}
              title="Active"
              cells={{ data: activeCheckbox }}
              width="165px"
            />

            <GridColumn
              {...props.columnPropsBuilder("NOTES")}
              title="Notes"
              cells={{ data: OrganizationCell }}
              width="360px"
            />

            <GridColumn
              title="Contact Focals"
              cells={{ data: ContactFocals }}
              width="200px"
            />
          </AnyGrid>
        </Card>
      </div>
    );
  }
);

export default OrganizationTable;
