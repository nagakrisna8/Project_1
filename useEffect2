const startCoords = ed.view.coordsAtPos(start);
const endCoords = ed.view.coordsAtPos(end);

// middle horizontally
const x = (startCoords.left + endCoords.left) / 2;

// top of selection (works for multi-line)
const y = Math.min(startCoords.top, endCoords.top) - 35 + window.scrollY;
