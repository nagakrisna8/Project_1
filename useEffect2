React.useEffect(() => {

    if (props.focalData && props.focalData.length > 0) {

        const lastItem = props.focalData[props.focalData.length - 1];

        if (lastItem?.inEdit) {

            setDetailExpand(prev => ({
                ...prev,
                [lastItem.ORGANIZATIONID]: true
            }));

        }
    }

}, [props.focalData]);
