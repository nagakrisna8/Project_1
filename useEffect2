useEffect(() => {

    if (props.focalData?.length) {

        const newRow = props.focalData[0];

        if (newRow?.newFlag) {

            setDetailExpand(prev => ({
                ...prev,
                [newRow.ID]: true
            }));

        }

    }

}, [props.focalData]);
