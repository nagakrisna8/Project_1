useEffect(() => {

    if (props.focalData?.length) {

        const firstRow = props.focalData[0];

        setDetailExpand({
            [firstRow.ID]: true
        });

    }

}, [props.focalData]);
