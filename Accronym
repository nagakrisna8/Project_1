const cvehandleChange = async (event: SelectChangeEvent<string[]>) => {
    if (!checkUnsavedChanges()) {
        const confirmed = await userDefinedConfirm({
            message: "There are unsaved changes in the current session. Do you want to proceed without saving?",
            okBtnText: "Yes",
            cancelBtnText: "Cancel",
        });

        if (confirmed) {
            _handleDirtyFlag(false);
        } else {
            return;
        }
    }

    const newValue = event.target.value as string;
    const finalSelected: number[] = [];

    if (newValue === "All") {
        gridGeneratedCvePanels.forEach(x => finalSelected.push(x.Cve_Id));
    } else {
        finalSelected.push(parseInt(newValue));
    }

    setSelectCve(event.target.value as string[]);

    try {

        const CdiId =
            Array.isArray(gridFilterSelectedCDI) && gridFilterSelectedCDI.length > 0
                ? gridFilterSelectedCDI.join(",")
                : 0;

        const resp = await CclBuilderGridService.GetcdiById(
            finalSelected.join(","),
            CdiId
        );

        const filteredCDI = resp.data;

        // Update CDI dropdown
        setSecondCDI(filteredCDI);

        // Keep previously selected CDI ids that still exist
        const selectedIds = selectCDI.filter(id =>
            filteredCDI.some(cdi => cdi.Cdi_Id === id)
        );

        setSelectCDI(selectedIds);

        // Automatically check matching items in second dropdown
        const selectedDescriptions = filteredCDI
            .filter(cdi => selectedIds.includes(cdi.Cdi_Id))
            .map(cdi => cdi.Combined_Description);

        if (selectedDescriptions.length > 0) {
            setSecondSelectedCDI(
                selectedDescriptions as unknown as string[]
            );
        } else {
            setSecondSelectedCDI([] as unknown as string[]);
        }

        const reportId = localStorage.getItem("OSS-CertToolReportId")!;

        await fetchCclGridData(
            { skip: 0, take: 50 },
            "cvecdi",
            reportId,
            finalSelected.join(","),
            selectedIds.join(",")
        );

    } catch (error) {
        dispatch(
            showSnackbar({
                snackbarOpen: true,
                snackbarType: "error",
                snackbarMessage: "Error getting CVE Data.",
            })
        );
    }
};
