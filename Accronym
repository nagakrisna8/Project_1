useEffect(() => {
    if (
        selectedPage === "Focal Management" &&
        selectedOrganizationId &&
        selectedOrgId
    ) {
        fetchFocal(selectedOrganizationId, selectedOrgId);
    }
}, [selectedPage, selectedOrganizationId, selectedOrgId]);


const onFocalDataStateChange = (event: GridDataStateChangeEvent) => {
    setSupplierActivityDataState(event.dataState);
};
