 const breadcrumbContainer = document.getElementById('oss-menu-breadcrums') || document.getElementById('oss-myworkspace-breadcrums');
    const storageServiceTemp = new StorageService();
    const isFromNavigation = storageServiceTemp.getApplicationDataByKey(`${process.env.REACT_APP_ROOT_NAME}-${process.env.REACT_APP_FIND_CSR}`, "isfromnavigation");
    let originalTitle = storageServiceTemp.getApplicationDataByKey(`${process.env.REACT_APP_ROOT_NAME}-easaActiveSubMenu`, 'easaActiveSubMenuTitle');
    let isEasactiveSubmenu = storageServiceTemp.getApplicationDataByKey(`${process.env.REACT_APP_ROOT_NAME}-easaActiveSubMenu`, 'easaactivesubmenu') !== "projectDetailsBttn";
    let isSubMenuCertStrategy = storageServiceTemp.getApplicationDataByKey(`${process.env.REACT_APP_ROOT_NAME}-easaActiveSubMenu`, 'easaactivesubmenu') === "cclBuilderBttn";
    const isFromCertStrategy = (isFromNavigation && originalTitle && (originalTitle === "Airworthiness") && !isSubMenuCertStrategy);
    const isCsrListSearchRoute = window.location.hash.includes(WorkAppRoutes.FIND_CSR.CSR_LIST_SEARCH) || window.location.hash.includes(WorkAppRoutes.FIND_CSR.CSR_REPORT);
    const $breadcrumb = $('#oss-menu-breadcrums');
    if ($breadcrumb.length && !$breadcrumb.find('.cert-strategy-item').length && isFromCertStrategy && isEasactiveSubmenu && isCsrListSearchRoute) {
        const $arrow = $('<div>')
        const $label = $('<div>').addClass("breadcrumsStyles cert-strategy-item").text("Cert Strategy")
            .click(() => {
                EASAMenuSubmenu.handleSubMenuButtonClick("cclbuilderbttn");
            });
        const $arrow2 = $('<div>').addClass("gg-chevron-right cert-strategy-item");
        const $airworthiness = $breadcrumb.find(':contains("Airworthiness")').first();
        if ($airworthiness.length) {
            $airworthiness.before($arrow, $label, $arrow2);
        } else {
            $breadcrumb.append($arrow).append($label).append($arrow2);
        }
    }
