useEffect(() => {
    if (props.focalData?.length) {
        const newRow = props.focalData.find(
            row => row.newFlag && (!row.NAME || row.NAME === "")
        );

        if (newRow) {
            setDetailExpand({
                [newRow.ID]: true
            });
        }
    }
}, [props.focalData]);
