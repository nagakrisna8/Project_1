useEffect(() => {
    if (
        selectedPage === "Focal Management" &&
        organizationData.length > 0
    ) {
        setSelectedOrganizationId(organizationData[0].ORGANIZATIONID);
    }
}, [selectedPage, organizationData]);
