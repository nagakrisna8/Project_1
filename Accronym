const cancelFocal = (id: number) => {
    const row = props.focalData.find((item: any) => item.ID === id);

    // ✅ ONLY for new row → remove it
    if (row?.newFlag) {
        props.focalData.splice(
            props.focalData.findIndex((item: any) => item.ID === id),
            1
        );
    }

    // collapse row
    setDetailExpand(prev => ({
        ...prev,
        [id]: false
    }));
};


