useEffect(() => {
    if (props.focalData?.length) {
        const newRow = props.focalData.find(row => row.newFlag);

        if (newRow) {
            setDetailExpand({
                [newRow.ID]: true
            });
        }
    }
}, [props.focalData]);

