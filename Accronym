const filteredCDI = resp.data;

// Update the filtered CDI list
setSecondCDI(filteredCDI);

// Check ALL returned CDI items by default
const selectedIds = filteredCDI.map(item => item.Cdi_Id);
setSelectCDI(selectedIds);

// Select all descriptions for the second dropdown
const selectedDescriptions = filteredCDI.map(
    item => item.Combined_Description
);

setSecondSelectedCDI(selectedDescriptions as unknown as string[]);


await fetchCclGridData(
    { skip: 0, take: 50 },
    "cvecdi",
    reportId,
    finalSelected.join(","),
    selectedIds.join(",")
);
