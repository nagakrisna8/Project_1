const handleCreateFocalFromOutside = () => {
    setSelectedPage("Focal Management"); // ✅ switch tab

    // small delay to ensure component mounts
    setTimeout(() => {
        CreateFocal(); // ✅ trigger existing logic
    }, 0);
};


  onCreateFocal={handleCreateFocalFromOutside}

<FocalList onCreateFocal={props.onCreateFocal} />
