useEffect(() => {
    if (
        selectedPage === "Focal Management" &&
        selectedOrganizationId &&
        selectedOrgId
    ) {
        const load = async () => {
            await fetchFocal(selectedOrgId, selectedOrganizationId);

            if (shouldAutoCreateFocal) {
                const newFocal = {
                    ID: new Date().getTime(),
                    NAME: "",
                    PHONENO: "",
                    ADDRESS: "",
                    EMAIL: "",
                    POSITION: "",
                    TITLE: "",
                    newFlag: true,
                    SORT_ORDER: 1,
                    ISACTIVE: true,
                };

                setFocalData(prev => [newFocal, ...prev]);
                setShouldAutoCreateFocal(false);
            }
        };

        load();
    }
}, [selectedPage, selectedOrganizationId, selectedOrgId, shouldAutoCreateFocal]); // ✅ FIX
