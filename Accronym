const observer = new MutationObserver(() => {
  const btn = document.getElementById("createNewBtn");
  if (btn) {
    console.log("Button appeared!", btn);
  }
});

observer.observe(document.body, {
  childList: true,
  subtree: true
});
