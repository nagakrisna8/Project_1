// AFTER
const initialValue =
    props.dataItem[canonicalField] ??
    props.dataItem["TaskId"] ??
    props.dataItem["Task"] ??
    "";
const normalizedTasks = initialValue && String(initialValue).trim() !== "" && String(initialValue).trim() !== "0"
    ? String(initialValue)
        .split(",")
        .map((x) => Number(x.trim()))
        .filter((x) => !isNaN(x) && x !== 0)
    : [];


// AFTER
value: value.length > 0 ? value.join(",") : "",
