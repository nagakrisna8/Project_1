 <TextField
                          variant="standard"
                          value={searchText}
                          style={{ width: "295px" }}
                          // onChange={handleSearchUser}
                          onChange={getName}
                          onKeyPress={onKeyUp}
                          placeholder="Last Name, First Name/BEMS ID"
                          className="kgrid-oss-csr-lookup-popup-input"
                          inputProps={{ style: { fontSize: 14, color: '#253746', fontWeight: 'normal' } }}
                          InputProps={{
                            endAdornment: (
                              <InputAdornment position="end">
                                <IconButton className="kgrid-oss-csr-lookup-popup-search-icon" onClick={() => handleSearchTextChange(searchText)}><SearchIcon /></IconButton>
                              </InputAdornment>
                            ),
                          }}
                        />
