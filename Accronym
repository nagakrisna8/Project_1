const observer = new MutationObserver(() => {
  const btn = document.getElementById("createNewBtn"); // 👈 your real id

  if (btn) {
    btn.click();
    observer.disconnect(); // stop observing
  }
});

// start observing whole DOM
observer.observe(document.body, {
  childList: true,
  subtree: true,
});
