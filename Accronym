const storageService = new StorageService();

const reportTemplate = storageService.getApplicationDataByKey(
    `${process.env.REACT_APP_ROOT_NAME}-${process.env.REACT_APP_FIND_CSR}`,
    "reportTemplate"
);

templateIdDetail = reportTemplate?.TemplateId;
groupIdDetail = reportTemplate?.GroupId;

if (!templateIdDetail) return "";
