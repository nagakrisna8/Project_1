useEffect(() => {
    if (
        selectedPage === "Focal Management" &&
        selectedOrganizationId &&
        selectedOrgId
    ) {
        // ❌ skip fetch if we just created new row
        if (!focalData.some(row => row.newFlag)) {
            fetchFocal(selectedOrgId, selectedOrganizationId);
        }
    }
}, [selectedPage, selectedOrganizationId, selectedOrgId]);
