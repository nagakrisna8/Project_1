// ✅ Step 1: remove newFlag row from grid
if (clearFormData) clearFormData();

if (cancelFocal && focalData?.ID !== undefined) {
    cancelFocal(focalData.ID);
}

// ✅ Step 2: fetch fresh data
fetchFocal?.(selectedOrgId, selectedOrganizationId);
