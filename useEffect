  if (current.some((i) => !i.ACTIVITYCATEGORY || i.ACTIVITYCATEGORY.trim() === "")) {
            dispatch(showSnackbar({
                snackbarOpen: true,
                snackbarType: "error",
                snackbarMessage: "Please fill details.",
            }));
            return;
        }

        const uniqueCount = new Set(
            current.map((i) => i.ACTIVITYCATEGORY.trim().toLowerCase())
        ).size;

        if (uniqueCount !== current.length) {
            dispatch(showSnackbar({
                snackbarOpen: true,
                snackbarType: "error",
                snackbarMessage: "Supplier Activity must be unique.",
            }));
            return;
        }
